# RAG for Law: Интеллектуальная система для консультаций по ПДД РФ

## Описание проекта

**RAG for Law** — это интеллектуальная система, построенная на архитектуре **Corrective Retrieval-Augmented Generation (CRAG)**, для предоставления точных и обоснованных ответов на вопросы о Правилах Дорожного Движения (ПДД) Российской Федерации. В отличие от стандартного RAG, система не просто извлекает информацию, но и **критически оценивает** ее полноту, при необходимости **выполняет корректирующий поиск** и **синтезирует** итоговый ответ. Это обеспечивает высокую релевантность и достоверность предоставляемой информации.

---

## 🏗️ Архитектура пайплайна

Архитектура проекта построена вокруг модульного пайплайна, который обрабатывает запрос пользователя в два основных этапа: **извлечение релевантной информации** и **многоэтапная генерация ответа с самокоррекцией**.

### Общая схема работы

```
Пользовательский Запрос
        ↓
    RAGPipeline (Оркестратор)
        ↓
┌───────────────────────┐
│   1. Извлечение (Retrieval)   │
│   PDDRetriever                  │
│   - Трехуровневый семантический поиск  │
│   - FAISS + Sentence Transformers │
└───────────────────────┘
        ↓
┌───────────────────────┐
│   2. Corrective Generation (CRAG) │
│   - Каскад из 5 шагов рассуждения      │
│   - LLM (Mistral-7B-Instruct-v0.2)    │
└───────────────────────┘
        ↓
    Финальный Ответ + Источники
```

### 1. Извлечение Релевантной Информации (`PDDRetriever`)

Этот компонент отвечает за поиск наиболее релевантных фрагментов текста из ПДД на основе пользовательского запроса.

*   **Технологии**: `sentence-transformers` для создания эмбеддингов, `FAISS` для быстрого векторного поиска.
*   **Алгоритм поиска**:
    1.  **Поиск по разделам**: Система находит наиболее релевантные разделы ПДД, сравнивая запрос с векторными представлениями названий и описаний разделов.
    2.  **Поиск по пунктам**: Выполняется поиск по всем пунктам и подпунктам во всем документе.
    3.  **Фильтрация и Ранжирование**: Результаты поиска по пунктам фильтруются так, чтобы остались только те, что принадлежат к топ-релевантным разделам. Затем они пере-ранжируются с помощью комбинированной оценки (сходство с разделом * сходство с пунктом).
*   **Результат**: Список объектов `Document` из LangChain, каждый из которых содержит текст пункта и метаданные (номер раздела, пункта, оценки релевантности).

### 2. Генерация Ответа с Corrective RAG (`create_sgr_chain`)

Это ядро системы, где реализована логика **Corrective RAG**. Процесс следует паттерну **Cascade** (каскад) и состоит из 5 шагов, каждый из которых структурирован с помощью Pydantic-схем.

1.  **Initial Generation (Начальная генерация)**: На основе найденного контекста LLM генерирует предварительный ответ и краткое рассуждение.
2.  **Critique and Decomposition (Критика и декомпозиция)**: **[Ключевой шаг CRAG]** LLM, выступая в роли критика, анализирует предварительный ответ и контекст. Он находит пробелы, неточности и формулирует 1-3 новых, более конкретных поисковых запроса для их устранения.
3.  **Iterative Retrieval (Итеративный поиск)**: **[Корректирующее действие CRAG]** Если критик нашел пробелы, система выполняет дополнительный поиск по новым запросам, чтобы собрать больше релевантного контекста.
4.  **Final Synthesis (Финальный синтез)**: LLM синтезирует финальный, полный и точный ответ, используя как исходный, так и дополнительно найденный контекст.
5.  **Factual Verification and Correction (Проверка фактов и коррекция)**: На последнем этапе LLM проверяет, что финальный ответ на 100% соответствует предоставленному контексту, и исправляет любые неточности или выдуманные факты.

Этот многоэтапный процесс позволяет системе не просто выдать ответ, а "подумать" над ним, критически оценить и при необходимости доработать, что значительно повышает качество и надежность результата.

---

## 🔄 Взаимодействие Компонентов

1.  **Точка входа (`main.py`)**:
    *   Скрипт `main.py` является консольным интерфейсом для взаимодействия с системой.
    *   Он парсит аргументы командной строки (запрос, количество документов, флаг трассировки) и инициализирует главный класс `RAGPipeline`.

2.  **Оркестратор (`src/pipeline.py`)**:
    *   Класс `RAGPipeline` является центральным узлом, который управляет всем процессом.
    *   При инициализации он загружает LLM (`mistralai/Mistral-7B-Instruct-v0.2`) и создает экземпляр `PDDRetriever`.
    *   Метод `run(query)` запускает полный пайплайн: вызывает `retriever.search()` для получения контекста, а затем передает контекст и запрос в CRAG-цепочку (`sgr_chain.invoke()`).

3.  **Логика рассуждений (`src/chains.py`)**:
    *   Функция `create_sgr_chain()` создает сложную цепочку рассуждений, реализующую CRAG.
    *   Она использует набор промптов (шаблонов) для каждого из 5 шагов и `RobustJsonOutputParser` для надежного извлечения структурированного JSON-ответа от LLM.
    *   Внутри `sgr_logic` реализована вся каскадная логика, включая условный переход к итеративному поиску.

4.  **Структурирование данных (`src/schemas.py`)**:
    *   Pydantic-модели (`InitialGenerationOutput`, `CritiqueOutput` и др.) определяют строгую структуру данных для каждого шага рассуждения.
    *   Это обеспечивает предсказуемость и надежность при обмене данными между шагами CRAG-цепочки.

5.  **Извлечение данных (`src/retriever.py`)**:
    *   Класс `PDDRetriever` инкапсулирует всю логику поиска. Он загружает данные из `data/pdd.json`, создает или загружает из кэша FAISS-индексы и предоставляет метод `search()` для выполнения трехуровневого поиска.

---

## 📦 Список зависимостей

Для установки всех необходимых библиотек выполните команду:

```bash
pip install -r requirements.txt
```

Основные зависимости проекта и их назначение:

*   `torch`: Фреймворк для глубокого обучения, основа для трансформерных моделей.
*   `transformers`: Библиотека от Hugging Face для работы с предобученными моделями (LLM, токенизаторы).
*   `accelerate`: Упрощает запуск моделей на различных аппаратных конфигурациях (CPU, GPU, multi-GPU).
*   `bitsandbytes`: Для 4-битной квантизации моделей, что позволяет запускать большие модели на оборудовании с ограниченным объемом видеопамяти.
*   `langchain-core`, `langchain-huggingface`: Компоненты фреймворка LangChain для построения пайплайнов.
*   `sentence-transformers`: Для создания качественных текстовых эмбеддингов.
*   `faiss-cpu`: Библиотека для эффективного векторного поиска.
*   `pydantic`: Для валидации и структурирования данных с помощью Python-схем.
*   `numpy`: Для научных вычислений и работы с массивами.
*   `python-dotenv`: Для управления переменными окружения (например, ключами API).
*   `rouge-score`: Для автоматической оценки качества генерации с помощью метрики ROUGE.

---

## 📁 Структура Проекта

```
.
├── main.py                 # Консольный интерфейс для запуска системы
├── evaluate.py             # Скрипт для оценки качества системы (ROUGE, LLM Judge)
├── requirements.txt        # Список зависимостей Python
├── README.md              # Этот файл
├── .gitignore             # Файл для исключения ненужных файлов из Git
├── data/                  # Директория с данными
│   ├── pdd.json           # Основной файл с структурированными ПДД
│   └── validation_set.jsonl # Набор данных для валидации
├── src/                   # Исходный код проекта
│   ├── __init__.py
│   ├── pipeline.py        # Основной класс RAGPipeline (оркестратор)
│   ├── retriever.py       # Класс PDDRetriever (семантический поиск)
│   ├── chains.py          # Логика CRAG-цепочки и промпты
│   └── schemas.py         # Pydantic-схемы для структурированных данных
└── faiss_langchain_cache/ # Кэш FAISS-индексов (создается автоматически)
    ├── sections_index.faiss
    ├── content_index.faiss
    └── content_metadata.pkl
```

---

## 🚀 Как Запустить

### Интерактивный режим

Просто запустите скрипт без аргументов:

```bash
python main.py
```

Система войдет в интерактивный режим, где вы сможете вводить вопросы по ПДД.

### Режим одного запроса

Вы можете передать вопрос напрямую в командной строке:

```bash
python main.py "Кто имеет преимущество на нерегулируемом перекрестке?"
```

### Дополнительные опции

*   `--top_k N`: Указать количество извлекаемых документов (по умолчанию 5).
*   `--trace`: Показать полную трассировку рассуждений CRAG-цепочки.

Пример:
```bash
python main.py "Разрешен ли разворот на мосту?" --top_k 3 --trace
```

---

## 📊 Оценка Качества

Для оценки качества ответов системы используется скрипт `evaluate.py`, который поддерживает два подхода:

1.  **ROUGE**: Автоматическая метрика для сравнения сгенерированного ответа с эталонным.
2.  **LLM as a Judge**: Использование другой языковой модели в качестве эксперта для оценки релевантности, добросовестности, полноты и ясности ответа.

Запуск оценки с помощью LLM-судьи:
```bash
python evaluate.py --use_llm_judge
```
