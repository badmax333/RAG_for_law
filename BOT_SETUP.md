# Инструкция по запуску Telegram бота RAG for Law

## 📋 Предварительные требования

Выберите один из вариантов работы с LLM:
   - **Mistral API** (рекомендуется) - требует API ключ, работает быстро, минимальные требования к RAM
   - **Локальная модель** - бесплатно, но требует минимум 14 GB RAM и мощный процессор

---

> **Альтернатива**: Если хотите использовать локальную модель без API (требует 14+ GB RAM), установите в `.env` параметр `USE_API=false`

Убедитесь, что `USE_API=true` (должно быть по умолчанию)


### Установка зависимостей

Просто запустите `run_bot.bat` - скрипт сам проверит и установит недостающие зависимости.

#### Вариант B: Ручная установка

```bash
# Активируйте виртуальное окружение
.venv\Scripts\activate  # Windows
# или
source .venv/bin/activate  # Linux/Mac

# Установите зависимости
pip install -r requirements.txt
```

### Шаг 5: Запуск бота

#### Windows:
Просто запустите файл `run_bot.bat` двойным кликом.

#### Linux/Mac:
```bash
source .venv/bin/activate
python bot.py
```

---

## 🎯 Основные команды бота

- `/start` - Приветствие и инструкция
- `/help` - Справка по командам
- `/history` - Последние 10 вопросов
- `/clear` - Очистить историю диалога
- `/export` - Экспортировать историю в .txt файл

**Просто отправьте текстовый вопрос**, чтобы получить ответ на основе ПДД РФ.

---

## ⚙️ Настройки (файл .env)

| Параметр | Описание | Значение по умолчанию |
|----------|----------|-----------------------|
| `TELEGRAM_BOT_TOKEN` | Токен бота от BotFather | *обязательно* |
| `USE_API` | Использовать API (`true`) или локальную модель (`false`) | `true` |
| `MISTRAL_API_KEY` | API ключ от Mistral AI (при USE_API=true) | *обязательно при API* |
| `MISTRAL_MODEL` | Модель Mistral (small/medium/large) | `mistral-small-latest` |
| `LOG_LEVEL` | Уровень логирования | `INFO` |
| `TOP_K_DEFAULT` | Количество релевантных документов | `5` |
| `HISTORY_MAX` | Максимум сообщений в истории | `30` |
| `LLM_TIMEOUT_SEC` | Таймаут генерации ответа (сек) | `35` |
| `PDD_PATH` | Путь к данным ПДД | `data/pdd.json` |
| `FAISS_CACHE_DIR` | Директория для FAISS кеша | `faiss_langchain_cache` |

### Доступные модели Mistral:

- **mistral-small-latest** - быстро и дешево (~$0.2/1M токенов)
- **mistral-medium-latest** - баланс качества и цены (~$2.5/1M токенов)
- **mistral-large-latest** - максимальное качество (~$8/1M токенов)

---